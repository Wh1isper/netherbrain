# pytest.ini
[pytest]
asyncio_mode = auto
testpaths = ["tests"]
markers =
    integration: requires Docker (PostgreSQL + Redis containers)
filterwarnings =
    error
    ignore::DeprecationWarning:testcontainers
    ignore::requests.exceptions.RequestsDependencyWarning
