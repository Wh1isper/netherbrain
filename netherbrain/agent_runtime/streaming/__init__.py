"""Streaming layer -- protocol adapters for converting internal events.

This package contains the protocol adapter interface and implementations
that convert internal SDK events to external protocol events (AG-UI).
"""
